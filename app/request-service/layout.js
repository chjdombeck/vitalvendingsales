export const metadata = {
  title: 'Request Service | Vital Vending Sales',
  description: 'Need a repair, refill, or maintenance visit? Request service on your vending machine or smart cooler and our team will get back to you fast.',
};

export default function RequestServiceLayout({ children }) {
  return children;
}
